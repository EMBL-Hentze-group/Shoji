stages:
  - test
  - build
  - deploy

# YAML anchor for reusable before_script
.poetry_setup: &poetry_setup
  before_script:
    - pip install poetry
    - poetry config virtualenvs.create false
    - poetry install

unit_test:
  stage: test
  image: python:3.12
  <<: *poetry_setup  # Include the reusable before_script
  script:
    - poetry run pytest
  
build_whl:
  stage: build
  needs:
    - unit_test
  image: python:3.12
  <<: *poetry_setup
  script:
    - poetry build --format wheel
  artifacts:
    paths:
      - dist/*.whl
    expire_in: 3 days

build_image:
  stage: deploy
  needs:
    - build_whl
  dependencies:
    - build_whl
  image:
    name: gcr.io/kaniko-project/executor:debug
    entrypoint: [""]
  script:
    - echo "{\"auths\":{\"$CI_REGISTRY\":{\"username\":\"$CI_REGISTRY_USER\",\"password\":\"$CI_REGISTRY_PASSWORD\"}}}" > /kaniko/.docker/config.json
    - /kaniko/executor --context $CI_PROJECT_DIR --dockerfile $CI_PROJECT_DIR/Dockerfile --destination ${CI_REGISTRY_IMAGE}:$CI_COMMIT_SHORT_SHA